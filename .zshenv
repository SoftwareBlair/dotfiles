# NVM directory (official installer + brew fallback)
export NVM_DIR="${NVM_DIR:-$HOME/.nvm}"

# Starship config from this dotfiles repo
export STARSHIP_CONFIG="${STARSHIP_CONFIG:-$HOME/dotfiles/.config/starship.toml}"
