# Generated by dotfiles-setup — eza helpers (when aliases module not selected)

if command -v eza >/dev/null 2>&1; then
  alias l='eza --all --binary --long --group --header --git --group-directories-first --icons'
fi
