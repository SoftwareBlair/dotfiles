# Generated by dotfiles-setup — orchestrator (do not edit; use ~/.zshrc.local)
# Modules are sourced from ~/.dotfiles-setup/generated/

if [[ -f "$HOME/.dotfiles-setup/shell-features.zsh" ]]; then
  # shellcheck disable=SC1090
  source "$HOME/.dotfiles-setup/shell-features.zsh"
fi

GEN="${DOTFILES_GENERATED_DIR:-$HOME/.dotfiles-setup/generated}"

{{MODULE_SOURCES}}

# User overrides (never overwritten by setup)
[[ -f "$HOME/.zshrc.local" ]] && source "$HOME/.zshrc.local"

# Non-login interactive shells never read ~/.zprofile — load secrets here
if [[ ! -o login && -f "$HOME/.zprofile" ]]; then
  # shellcheck disable=SC1090
  source "$HOME/.zprofile"
fi
